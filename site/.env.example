# GA4 measurement ID. Unset by default, so local builds and forks emit no
# analytics tag. The tag also refuses to load on any host but the configured
# `site`, so setting this in a fork still reports nothing.
PUBLIC_ANALYTICS_ID=
